<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TopNavigation"
            android:label="@string/title_activity_top_navigation"></activity>
        <activity android:name=".ContenedorActivity" />
        <!-- <activity android:name=".RadioActivity"></activity> -->
        <activity android:name=".NewPost" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.fragment.RecyclerListaUsuario" />
        <activity android:name=".RegistrarUsuario" />
        <activity android:name=".ui.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>